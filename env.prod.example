# ssrok Server Production Configuration Example
# Rename this file to .env on your production server

# TLS Configuration
# Set to 'true' to enable TLS (valid certs required)
# In production, usually handled by Nginx/Load Balancer or cloud provider (e.g., AWS ALB, Cloudflare)
# If false, the server runs in HTTP mode (behind a proxy).
SSROK_ENABLE_TLS=false

# Port to listen on (Default: 8080)
PORT=8080

# Production Server URL for Client (The URL clients will connect to)
# This value acts as the public entry point for your tunnel server.
SSROK_SERVER=https://tunnel.example.com

# Log Level (debug, info, warn, error)
LOG_LEVEL=info

# Rate Limiting (requests per minute per IP)
SSROK_RATE_LIMIT=120

# Redis Configuration (optional)
# If REDIS_HOST is set, sessions will be stored in Redis instead of memory
REDIS_HOST=
REDIS_PORT=6379
REDIS_USERNAME=
REDIS_PASSWORD=
